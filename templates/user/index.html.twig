{% extends 'base.html.twig' %}

{% block title %}Profil !{% endblock %}

{% block body %}
<img src="{{ asset('uploads/images/' ~ app.user.image) }}" alt="User Image" class="rounded-circle" style="max-width: 100px; max-height: 100px;">
{{ app.user.name }}
{% endblock %}


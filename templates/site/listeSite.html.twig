{% extends 'base.html.twig' %}

{% block title %}Site disponible{% endblock %}

{% block body %}
    <h1>Liste des sites disponible</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Url</th>
                <th>Category</th>
                {% if 'ROLE_ADMIN' in app.user.roles %}
                    <th>Identifiant</th>
                    <th>id Identifiant</th>
                    <th>class Identifiant</th>
                    <th>Mot de pass</th>
                    <th>id Mot de pass</th>
                    <th>class Mot de pass</th>
                    <th>class bouton</th>
                {% endif %}
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for site in sites %}
            <tr>
            {# {% if app.user.hasPermissionToVisit(site) %} #}
                <td>{{ site.id }}</td>
                <td>{{ site.name }}</td>
                <td>
                    <a class="link-success  link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover" href="{{ site.url }}" target="_blank">{{ site.url }}</a>
                    {# <a href="{{ path('connect_facebook_start', {'service': site.name}) }}">Connecter</a> #}
                </td>
                <td>{{ site.categorySite.name }}</td>
               
                {% if 'ROLE_ADMIN' in app.user.roles %}
                    <td>{{ site.user.email }}</td>

                    {# id login #}
                    {% if site.name =='Facebook' %}
                        <td>email</td>
                    {% elseif site.name == 'Instagram' %}
                        <td>-</td>
                    {% elseif site.name == 'Twiter' %}
                        <td>-</td>
                    {% elseif site.name == 'GitHub' %}
                        <td>login_field</td>
                    {% else %}
                        <td>-</td>  
                    {% endif %}
                    {# id login #}

                    {# class login #}
                    {% if site.name =='Facebook' %}
                        <td>-</td>
                    {% elseif site.name == 'Instagram' %}
                        <td>_aa4b _add6 _ac4d _ap35</td>
                    {% elseif site.name == 'Twiter' %}
                        <td>r-30o5oe r-1dz5y72 r-13qz1uu r-1niwhzg r-17gur6a r-1yadl64 r-deolkf r-homxoj r-poiln3 r-7cikom r-1ny4l3l r-t60dpp r-fdjqy7</td>
                    {% elseif site.name == 'GitHub' %}
                        <td>form-control input-block js-login-field</td>
                    {% else %}
                        <td>-</td>
                    {% endif %}
                    {# class login #}

                    <td>{{ site.user.password }}</td>

                    {# id password #}
                    {% if site.name =='Facebook' %}
                        <td>pass</td>
                    {% elseif site.name == 'Instagram' %}
                        <td>-</td>
                    {% elseif site.name == 'Twiter' %}
                        <td>-</td>
                    {% elseif site.name == 'GitHub' %}
                        <td>password</td>
                    {% else %}
                        <td>-</td>
                    {% endif %}
                    {# id password #}

                    {# class password #}
                    {% if site.name =='Facebook' %}
                        <td>-</td>
                    {% elseif site.name == 'Instagram' %}
                        <td>_aa4b _add6 _ac4d _ap35</td>
                    {% elseif site.name == 'Twiter' %}
                        <td>-</td>
                    {% elseif site.name == 'GitHub' %}
                        <td>form-control form-control input-block js-password-field</td>
                    {% else %}
                        <td>-</td>
                    {% endif %}
                    {# class password #}

                    {# class boutton #}
                    {% if site.name =='Facebook' %}
                        <td>_42ft _4jy0 _6lth _4jy6 _4jy1 selected _51sy</td>
                    {% elseif site.name == 'Instagram' %}
                        <td>_acan _acap _acas _aj1- _ap30</td>
                    {% elseif site.name == 'Twiter' %}
                        <td>-</td>
                    {% elseif site.name == 'GitHub' %}
                        <td>btn btn-primary btn-block js-sign-in-button</td>
                    {% else %}
                        <td>-</td>
                    {% endif %}
                    {# class boutton #}
                    
                <td>
                    {# <a class="btn btn-info" href="{{ path('app_site_show', {'id': site.id}) }}">Afficher</a> #}
                    <a class="btn " href="{{ path('app_site_edit', {'id': site.id}) }}"><img src="{{ asset('uploads/icone/bouton-modifier.png') }} " alt="User Image" width="50" height="50"></a>
                    <a class="btn " href="{{ path('app_site_delete_direct', {'id': site.id}) }}"><img src="{{ asset('uploads/icone/supprimer (1).png') }} " alt="User Image" width="50" height="50"></a>
                </td>
                {% else %}
                <td>
                     <a class="btn btn-outline-primary" href="#">Aficher</a>
                </td>
                {% endif %}
            {# {% else %}
            {% endif %}             #}
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucun document trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% if 'ROLE_ADMIN' in app.user.roles %}
        <a class="btn btn-primary" href="{{ path('app_site_new') }}">Créer un nouveau</a>
    {% else %}
    {% endif %}
{% endblock %}

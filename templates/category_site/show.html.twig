{% extends 'base.html.twig' %}

{% block title %}CategorySite{% endblock %}

{% block body %}
    <h1>CategorySite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_site.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_site.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category_site.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_site_index') }}">back to list</a>

    <a href="{{ path('app_category_site_edit', {'id': category_site.id}) }}">edit</a>

    {{ include('category_site/_delete_form.html.twig') }}
{% endblock %}

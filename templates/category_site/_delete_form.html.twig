<form method="post" action="{{ path('app_category_site_delete', {'id': category_site.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_site.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

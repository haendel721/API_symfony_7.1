{% extends 'base.html.twig' %}

{% block title %}Type de Permission{% endblock %}

{% block body %}
    <h1>Type de Permission</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_permission.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_permission.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_permission.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_permission_index') }}">Retour à la list</a>

    <a href="{{ path('app_type_permission_edit', {'id': type_permission.id}) }}">Modifier</a>

    {{ include('type_permission/_delete_form.html.twig') }}
{% endblock %}
